# Copy to .env.local and fill in. Do not commit .env.local with secrets.

# —— Live URL (Vercel / production) ——
# Set in Vercel: Project → Settings → Environment Variables
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
# Or use custom domain: NEXT_PUBLIC_APP_URL=https://waypointlabs.io

# —— Backend API (optional; app works in demo mode without it) ——
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_WS_URL=ws://localhost:8000

# —— Ngrok (local tunnel) ——
# NGROK_AUTHTOKEN=...
# NGROK_DOMAIN=your-domain.ngrok.pro

# —— Optional ——
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=...
# NEXT_PUBLIC_CESIUM_ION_ACCESS_TOKEN=...
# NEXT_PUBLIC_ADMIN_ACCESS_CODE=WL-ADMIN-2024
