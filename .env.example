# Copy to .env.local and fill in. Do not commit .env.local with secrets.

# —— Frontend (Cloudflare Pages) ——
# Set in GitHub repo Variables (or Cloudflare build env) so manifest/OG use your live URL:
# NEXT_PUBLIC_APP_URL=https://waypoint-labs.pages.dev
# Or custom domain: NEXT_PUBLIC_APP_URL=https://waypointlabs.io

# —— Backend (Fly.io) ——
# Cloudflare build uses these to talk to Fly; local dev uses localhost.
# NEXT_PUBLIC_API_URL=https://civilian-drone-app.fly.dev
# NEXT_PUBLIC_WS_URL=wss://civilian-drone-app.fly.dev

# —— Local dev ——
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_WS_URL=ws://localhost:8000

# —— Ngrok (local tunnel) ——
# NGROK_AUTHTOKEN=...
# NGROK_DOMAIN=your-domain.ngrok.pro

# —— Optional ——
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=...
# NEXT_PUBLIC_CESIUM_ION_ACCESS_TOKEN=...
# NEXT_PUBLIC_ADMIN_ACCESS_CODE=WL-ADMIN-2024
